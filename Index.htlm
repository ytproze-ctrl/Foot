<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Live Foot – Résumés et Scores</title>
  <style>
    body { background: #0d1117; color: white; font-family: Arial; text-align: center; }
    .grid { display: flex; flex-direction: column; align-items: center; gap: 30px; }
    .card { width: 90%; max-width: 800px; background: #161b22; border-radius: 16px; padding: 20px; }
    iframe { width: 100%; height: 400px; border: none; border-radius: 10px; }
  </style>
</head>
<body>
  <h1>⚽ Live Foot – Résumés et Scores</h1>
  <div id="content" class="grid"></div>

  <script>
    fetch("links.json")
      .then(res => res.json())
      .then(data => {
        const content = document.getElementById("content");
        data.forEach(match => {
          const card = document.createElement("div");
          card.className = "card";
          card.innerHTML = `<h3>${match.title}</h3>
                            <iframe src="${match.embed}" allowfullscreen></iframe>`;
          content.appendChild(card);
        });
      });
  </script>
</body>
</html>

